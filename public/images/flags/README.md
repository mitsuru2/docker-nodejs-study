## flagsフォルダ

このフォルダには`country-flag`コンポーネントで使用する国旗画像を格納します。
1:1画像は使用しないため、4:3画像のみ取り込んでいます。

### ダウンロード元:

元データは`flag-icons`ライブラリのGithubリポジトリです。
`flag-icons`ライブラリ経由で国旗画像を表示するとHTMLとCSSの影響で意図しない余白が発生するため、元データのみをダウンロードして使用しています。
https://www.npmjs.com/package/flag-icons
